{
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": { 
        "distDir": "build",
        "buildCommand": "chmod +x node_modules/.bin/react-scripts && npm run build"
      }
    }
  ]
}